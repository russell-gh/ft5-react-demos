function Controls({ onInput, searchTerm }) {
  return (
    <input
      onChange={() => {}}
      type="text"
      onInput={onInput}
      value={searchTerm}
    />
  );
}

export default Controls;
